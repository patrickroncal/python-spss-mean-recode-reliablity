### Additional variable generator for mean

def AM(title,number):
    for x in xrange(1,number+1):
        if x != number:
            print title+str(x)+", "
        if x == number:
            print title+str(x)
